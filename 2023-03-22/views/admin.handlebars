<h1>Registruoti vartotojai</h1>
<table class="table">
    <thead>
        <tr>
            <th>Vardas</th>
            <th>Pavardė</th>
            <th>El. pašto adresas</th>
            <th>Slaptažodis</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        {{#each data}}
            <tr>
                <td>{{name}}</td>
                <td>{{last_name}}</td>
                <td>{{email}}</td>
                <td>{{password}}</td>
                <td>
                    <a href="/delete-user/{{@key}}" class="btn btn-danger">Ištrinti</a>
                </td>
            </tr>
        {{/each}}
    </tbody>
</table>